import React from 'react'
import 'enl-styles/Login/LoginContainer.css'

const LoginContainer = ({ children }) => (
  <div className='LoginContainer'>
    {children}
  </div>
)

export default LoginContainer
